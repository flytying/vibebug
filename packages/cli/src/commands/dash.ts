import pc from 'picocolors';

export async function dashCommand(options: { port?: string; open?: boolean }): Promise<void> {
  // Placeholder — will be implemented in M2.4
  console.log(pc.dim('Dashboard not yet implemented. (stub)'));
}
